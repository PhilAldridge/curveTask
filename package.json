{
  "name": "curve-task",
  "version": "1.0.0",
  "description": "NodeJS script to ingest spreadsheet data into MongoDB",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "test": "mocha test/**/*.test.js --timeout 3000",
    "dev": "nodemon src/index.js",
    "docker:build": "docker-compose build",
    "docker:up": "docker-compose up",
    "docker:down": "docker-compose down",
    "docker:test": "docker-compose run --rm app npm test"
  },
  "author": "Phil Aldridge",
  "dependencies": {
    "mongoose": "^7.5.0",
    "xlsx": "^0.18.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "chai": "^4.3.8",
    "nodemon": "^3.0.1",
    "mongodb-memory-server": "^9.1.1"
  }
}